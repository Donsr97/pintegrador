import { useState, useEffect } from 'react';
import ReactFlow, {
  ReactFlowProvider,
  addEdge,
  useNodesState,
  useEdgesState,
  Controls,
} from 'react-flow-renderer';
import {getCuad} from  "../RutasFunciones"
function Main() {

    return (
        <>

        </>
    )
}

export default Main
